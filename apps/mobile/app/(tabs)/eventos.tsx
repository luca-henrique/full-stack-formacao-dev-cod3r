import { textXl } from "@/style";
import { centerGrow } from "@/style/components";
import { Text, View } from "react-native";

export default function Index() {
  return (
    <View style={[centerGrow]}>
      <Text style={[textXl]}>Eventos.</Text>
    </View>
  );
}
